# =============================================================================
# print-mark Environment Configuration
# =============================================================================
# Copy this file to .env and customize as needed.
# All variables are optional and have sensible defaults.
# =============================================================================

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------

# Port for the HTTP server (default: 3000)
PORT=3000

# Bind address (default: 0.0.0.0 for all interfaces)
# Use 127.0.0.1 to restrict to localhost only
HOST=0.0.0.0

# Environment mode (default: production)
# Options: development, production, test
# - development: enables CORS for all origins
# - production: disables CORS (use reverse proxy)
NODE_ENV=development

# -----------------------------------------------------------------------------
# File Upload Configuration
# -----------------------------------------------------------------------------

# Maximum file upload size in bytes (default: 26214400 = 25MB)
# Increase for larger documents, decrease to limit resource usage
MAX_FILE_SIZE=26214400

# -----------------------------------------------------------------------------
# PDF Generation Configuration
# -----------------------------------------------------------------------------

# Timeout for PDF generation in milliseconds (default: 30000 = 30 seconds)
# Increase for very large documents
PDF_TIMEOUT=30000

# Maximum concurrent PDF generation requests (default: 3)
# Higher values use more memory but handle more simultaneous requests
# Each PDF generation requires a Chromium page instance
PDF_CONCURRENT_LIMIT=3

# -----------------------------------------------------------------------------
# Docker/Puppeteer Configuration (Set automatically in Docker)
# -----------------------------------------------------------------------------

# Path to Chromium executable (optional, for Docker environments)
# When set, Puppeteer uses system Chromium instead of bundled version
# PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium-browser
