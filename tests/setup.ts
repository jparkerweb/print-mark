import { beforeAll, afterAll } from 'vitest';

beforeAll(async () => {
  // Global test setup
  // Add any initialization needed before all tests run
});

afterAll(async () => {
  // Global test cleanup
  // Add any cleanup needed after all tests complete
});
